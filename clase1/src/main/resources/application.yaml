spring:
  application:
    name: miguel
  datasource:
    url: jdbc:mysql://localhost:3306/mydatabase
    username: ${username}
    password: ${password}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update